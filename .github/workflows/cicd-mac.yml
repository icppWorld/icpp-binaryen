name: cicd-mac

on:
  workflow_dispatch:
  push:
    paths:
      - "src/**"
      - "test/**"
      - "scripts/**"
      - "Makefile"
      - "pyproject.toml"
      - "setup.py"
      - ".github/workflows/cicd-mac.yml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  all:
    strategy:
      fail-fast: false
      matrix:
        # Quoted: an unquoted 3.10-style number would parse as a float
        python-version: ["3.11", "3.12", "3.13", "3.14"]
        os: [macos-15-intel, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: install
        run: |
          python -m pip install --upgrade pip
          make install-python

      - name: get-binaryen
        run: make get-binaryen

      - name: all-tests
        run: make all-tests

      - name: wheel smoke test
        run: |
          make pypi-build
          python -m venv /tmp/clean
          /tmp/clean/bin/pip install dist/*.whl pytest==8.3.4 wasmtime==29.0.0
          /tmp/clean/bin/pytest -vv test/smoke
